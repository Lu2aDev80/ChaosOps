# Local Development Environment Template
# Copy this file to .env and update with your settings

# Database Configuration (Docker production)
DATABASE_URL="postgresql://konfiadmin:konfi2024secure@postgres:5432/konfidayplaner?schema=public"

POSTGRES_USER=konfiadmin
POSTGRES_PASSWORD=konfi2024secure
POSTGRES_DB=konfidayplaner

# SMTP / Mail configuration (Use live mail server settings)
SMTP_HOST=mail.prepaid-mail.de
SMTP_PORT=587
SMTP_USER=info@chaos-ops.de
SMTP_PASS=infoatchaos-ops.de
SMTP_SECURE=false
SMTP_IGNORE_TLS=false
SMTP_REQUIRE_TLS=true
SMTP_REJECT_UNAUTHORIZED=false
MAIL_FROM_NAME=Chaos Ops
MAIL_FROM_EMAIL=info@chaos-ops.de

# Logging (more verbose for development)
LOG_LEVEL=debug
PRISMA_LOG=info


# Application configuration
APP_BASE_PATH=
NODE_ENV=production

# Frontend URL Configuration
# For production: Set to your production URL
FRONTEND_URL=https://chaos-ops.de
# Alternative: Leave empty for auto-detection from request headers

# For production: This should be your live domain
FRONTEND_HOST=https://chaos-ops.de

# API URL for frontend (Vite env var)
VITE_API_URL=https://chaos-ops.de/api

# Base URL for Vite build
VITE_BASE_URL=/
